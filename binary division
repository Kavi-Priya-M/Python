data =raw_input("Enter 4-digit binary numbers separated by comma: ")

binary_list = data.split(",")

for b in binary_list:
    decimal = int(b, 2)   # convert binary to decimal
    if decimal % 5 == 0:
        print(b)
